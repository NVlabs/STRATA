SCREAM_ZARR_PROFILE=XXX
PROJECT_ROOT=XXX
# Root directory for auxiliary (non-zarr) data files: SCRIP regridding .nc, ps .nc, latlon .nc, z coords, etc.
AUX_DATA_ROOT=XXX
# Root path for zarr datasets (S3 bucket prefix or lustre path)
ZARR_ROOT=XXX
# Full zarr paths derived from ZARR_ROOT
SCREAM_MAIN_ZARR_PATH=${ZARR_ROOT}/XXX.zarr
SCREAM_AUX_ZARR_PATH=${ZARR_ROOT}/XXX.zarr
WANDB_API_KEY=XXX
WANDB_PROJECT=XXX
WANDB_ENTITY=XXX
